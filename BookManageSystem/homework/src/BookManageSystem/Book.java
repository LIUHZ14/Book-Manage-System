package BookManageSystem;

public class Book {// Defines a public class named Book to represent a book in the library system
    String bookname;// Instance variable to store the name of the book
    String bookAuthor;// Instance variable to store the author of the book
    int bookPrice;// Instance variable to store the price of the book
}
